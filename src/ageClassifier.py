import tensorflow as tf

class ageClassifier(object):
	"""
		Age classifier Class
		Stores a pretrained age classifier model
		Implements a function to indentify the age of a person
	"""
	def __init__(self):
		super(ageClassifier, self).__init__()
		self.arg = arg
